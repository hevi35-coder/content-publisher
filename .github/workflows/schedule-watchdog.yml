name: Weekly Schedule Watchdog

on:
  schedule:
    # Run after expected 17:00 KST schedule (grace included)
    # Sunday 19:30 KST (Sunday 10:30 UTC)
    - cron: '30 10 * * 0'
    # Monday/Wednesday/Friday 19:30 KST (Monday/Wednesday/Friday 10:30 UTC)
    - cron: '30 10 * * 1,3,5'
  workflow_dispatch:

jobs:
  watchdog:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    env:
      TZ: Asia/Seoul
      WEEKLY_WORKFLOW_REF: '.github/workflows/weekly-content.yml'
      SCHEDULE_WATCHDOG_GRACE_MINUTES: '120'
      SCHEDULE_WATCHDOG_EARLY_ALLOWANCE_MINUTES: '15'
      SCHEDULE_WATCHDOG_API_MAX_ATTEMPTS: '3'
      SCHEDULE_WATCHDOG_API_RETRY_BASE_MS: '2000'
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm ci

      - name: Check Weekly Schedule Run Health
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/check-weekly-schedule-watchdog.js
